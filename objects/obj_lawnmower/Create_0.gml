sprite_scale = 1.4; // bigger than 1 = larger than original size
image_xscale = sprite_scale;
image_yscale = sprite_scale;

mounted = false;
move_speed_bonus = 1.5;
side_offset = 70;  // distance when facing left/right
fb_offset = 50;    // distance when facing front/back (and diagonals)